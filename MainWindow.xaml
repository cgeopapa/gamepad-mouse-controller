<Window x:Class="gamepad_mouse_controller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Background="{DynamicResource {x:Static SystemColors.WindowColor}}"
        Title="MainWindow" Height="450" Width="800" WindowStyle="SingleBorderWindow" WindowState="Minimized">

    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10" Orientation="Vertical">
            <Button FontSize="32" HorizontalAlignment="Stretch" Content="DISABLE CONTROLLER"></Button>
            <TextBox FontSize="20" Text="Currently: ACTIVE"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="10" Orientation="Vertical">
        </StackPanel>
    </Grid>
</Window>
